# 个人重要信息库 - 技术文档

## 📖 目录
1. [项目简介](#项目简介)
2. [工作原理](#工作原理)
3. [技术栈](#技术栈)
4. [架构设计](#架构设计)
5. [数据流](#数据流)
6. [功能模块详解](#功能模块详解)
7. [安全性分析](#安全性分析)
8. [常见问题](#常见问题)

---

## 项目简介

### 是什么？
**个人重要信息库** 是一个密码管理器。简单来说，就是一个**安全的笔记本**，用来记录你的各种账号和密码。

### 有什么特点？
- 🌐 **完全在浏览器运行** - 不需要安装任何软件
- 💾 **数据存在你的电脑里** - 不上传到任何服务器
- 📱 **一个 HTML 文件** - 打开就能用，没有复杂的安装过程
- 🔍 **功能完整** - 搜索、分类、导出、导入都有

### 适合谁用？
- 想管理自己的密码，但不信任云服务的人
- 需要一个简单、快速的密码管理工具的人
- 想要完全控制自己数据的人

---

## 工作原理

### 简单版本（5 秒理解）

```
你打开 HTML 文件
    ↓
在浏览器里看到一个表单
    ↓
填入网站名称、账号、密码
    ↓
点击"保存"
    ↓
数据存在你的浏览器里
    ↓
下次打开时，数据还在
```

### 详细版本

1. **打开应用**
   - 你在浏览器中打开 `index.html` 文件
   - 浏览器加载 HTML、CSS、JavaScript 代码
   - 应用启动，从浏览器存储中读取之前保存的数据

2. **添加数据**
   - 你填写表单（网站名称、账号、密码等）
   - 点击"保存"按钮
   - JavaScript 代码把数据转换成 JSON 格式
   - 数据保存到浏览器的 `localStorage`

3. **查看数据**
   - 应用从 `localStorage` 读取数据
   - 用 JavaScript 生成 HTML 代码
   - 在页面上显示所有记录

4. **下次打开**
   - 浏览器记住了你的数据
   - 下次打开时，数据自动加载
   - 就像从未关闭过一样

---

## 技术栈

### 什么是"技术栈"？
技术栈就是**用来构建这个应用的所有技术的组合**。

### 这个项目用了什么？

| 技术 | 作用 | 为什么选它 |
|------|------|---------|
| **HTML5** | 定义页面结构（表单、按钮、列表等） | 标准网页语言，所有浏览器都支持 |
| **CSS3** | 美化页面（颜色、布局、动画等） | 让应用看起来漂亮 |
| **JavaScript** | 实现功能（保存、搜索、删除等） | 让页面可以交互 |
| **localStorage API** | 在浏览器中存储数据 | 简单、安全、不需要服务器 |
| **Clipboard API** | 读写剪贴板 | 实现书签工具的复制功能 |

### 没有用什么？
- ❌ **没有数据库** - 不需要，数据存在浏览器里
- ❌ **没有服务器** - 不需要，一切都在本地
- ❌ **没有外部库** - 没有 React、Vue 等框架，代码更简洁
- ❌ **没有网络请求** - 不需要联网

---

## 架构设计

### 什么是"架构"？
架构就是**应用的整体结构**，像建筑的蓝图一样。

### 这个应用的架构

```
┌─────────────────────────────────────────┐
│         浏览器（你的电脑）               │
├─────────────────────────────────────────┤
│                                         │
│  ┌──────────────────────────────────┐  │
│  │      index.html（一个文件）       │  │
│  │                                  │  │
│  │  ┌────────────────────────────┐  │  │
│  │  │  HTML 结构                 │  │  │
│  │  │  - 表单                    │  │  │
│  │  │  - 列表                    │  │  │
│  │  │  - 按钮                    │  │  │
│  │  └────────────────────────────┘  │  │
│  │                                  │  │
│  │  ┌────────────────────────────┐  │  │
│  │  │  CSS 样式                  │  │  │
│  │  │  - 深色主题                │  │  │
│  │  │  - 响应式布局              │  │  │
│  │  │  - 动画效果                │  │  │
│  │  └────────────────────────────┘  │  │
│  │                                  │  │
│  │  ┌────────────────────────────┐  │  │
│  │  │  JavaScript 逻辑           │  │  │
│  │  │  - 保存数据                │  │  │
│  │  │  - 搜索过滤                │  │  │
│  │  │  - 删除编辑                │  │  │
│  │  │  - 导入导出                │  │  │
│  │  └────────────────────────────┘  │  │
│  │                                  │  │
│  └──────────────────────────────────┘  │
│                                         │
│  ┌──────────────────────────────────┐  │
│  │  浏览器存储（localStorage）       │  │
│  │  - 保存所有数据                  │  │
│  │  - 容量：5-10MB                  │  │
│  └──────────────────────────────────┘  │
│                                         │
└─────────────────────────────────────────┘
```

### 为什么这样设计？

**优点：**
- ✅ **简单** - 只有一个文件，没有复杂的依赖
- ✅ **快速** - 打开就能用，不需要加载
- ✅ **安全** - 数据不离开你的电脑
- ✅ **易分享** - 一个文件就能分享给别人
- ✅ **易备份** - 导出 JSON 文件就是备份

**缺点：**
- ❌ **不能同步** - 多个设备上的数据不会自动同步
- ❌ **容量有限** - localStorage 只有 5-10MB
- ❌ **不能加密** - 密码以明文存储（但数据不离开你的电脑）

---

## 数据流

### 添加一条记录的完整流程

```
用户在表单中填写信息
    ↓
点击"保存"按钮
    ↓
JavaScript 读取表单中的数据
    ↓
验证数据（至少填写名称或地址）
    ↓
创建一个对象：
{
  id: "1234567890",           // 唯一标识
  name: "GitHub",             // 网站名称
  url: "https://github.com",  // 网站地址
  user: "myusername",         // 账号
  pass: "mypassword",         // 密码
  note: "个人账号",           // 备注
  createdAt: "2026-05-28..."  // 创建时间
}
    ↓
把这个对象添加到 entries 数组
    ↓
调用 save() 函数
    ↓
把整个 entries 数组转换成 JSON 字符串
    ↓
保存到 localStorage['pwd_entries']
    ↓
调用 render() 函数
    ↓
JavaScript 生成 HTML 代码
    ↓
在页面上显示新记录
    ↓
显示"已添加"提示
```

### 搜索的流程

```
用户在搜索框输入关键词
    ↓
JavaScript 监听输入事件
    ↓
调用 getFiltered() 函数
    ↓
遍历所有记录
    ↓
检查每条记录的名称、地址、备注是否包含关键词
    ↓
同时检查时间范围（今天/本周/本月）
    ↓
返回匹配的记录列表
    ↓
调用 render() 函数
    ↓
只显示匹配的记录
```

---

## 功能模块详解

### 1. 数据管理模块

**职责：** 管理数据的增删改查

**关键函数：**
- `save()` - 把数据保存到 localStorage
- `saveEntry()` - 保存一条新记录或编辑现有记录
- `deleteEntry(id)` - 删除一条记录
- `editEntry(id)` - 打开编辑表单

**数据结构：**
```javascript
// 所有记录存在一个数组里
entries = [
  {
    id: "1234567890",
    name: "GitHub",
    url: "https://github.com",
    user: "username",
    pass: "password",
    note: "个人账号",
    createdAt: "2026-05-28T10:30:00.000Z"
  },
  {
    id: "0987654321",
    name: "Gmail",
    url: "https://gmail.com",
    user: "user@gmail.com",
    pass: "password123",
    note: "工作邮箱",
    createdAt: "2026-05-27T15:45:00.000Z"
  }
]
```

### 2. UI 渲染模块

**职责：** 把数据显示在页面上

**关键函数：**
- `render()` - 主渲染函数，更新整个列表
- `getFiltered()` - 获取过滤后的数据
- `updateStats()` - 更新统计信息

**工作流程：**
```
数据改变
    ↓
调用 render()
    ↓
调用 getFiltered() 获取要显示的数据
    ↓
用 JavaScript 生成 HTML 代码
    ↓
把 HTML 代码插入到页面
    ↓
用户看到更新后的页面
```

### 3. 搜索过滤模块

**职责：** 实现搜索和时间过滤

**支持的过滤方式：**
- 按关键词搜索（名称、地址、备注）
- 按时间范围（全部、今天、本周、本月）
- 两种过滤可以组合使用

**例子：**
```
搜索"GitHub" + 选择"本周"
    ↓
只显示本周添加的、名称包含"GitHub"的记录
```

### 4. 批量操作模块

**职责：** 实现批量选择、导出、删除

**工作流程：**
```
用户勾选记录前的复选框
    ↓
记录 ID 被添加到 selected Set
    ↓
更新"已选 X 条"的显示
    ↓
用户点击"批量导出"或"批量删除"
    ↓
对所有选中的记录进行操作
```

### 5. 导入导出模块

**职责：** 实现数据备份和恢复

**导出流程：**
```
用户点击"导出备份"
    ↓
JavaScript 把 entries 数组转换成 JSON
    ↓
创建一个 Blob 对象（二进制数据）
    ↓
浏览器下载这个文件
    ↓
文件名：info_backup_2026-05-28.json
```

**导入流程：**
```
用户选择一个 JSON 文件
    ↓
JavaScript 读取文件内容
    ↓
解析 JSON 数据
    ↓
检查每条记录的 ID，避免重复
    ↓
把新记录添加到 entries 数组
    ↓
保存到 localStorage
    ↓
显示"已导入 X 条新记录"
```

### 6. 书签工具模块

**职责：** 快速从网页保存信息

**工作流程：**
```
用户在任意网页点击书签
    ↓
书签执行 JavaScript 代码
    ↓
获取当前网页的标题和地址
    ↓
把这些信息复制到剪贴板
    ↓
页面右上角显示"已复制到剪贴板"提示
    ↓
用户回到信息库页面
    ↓
点击"📋 粘贴"按钮
    ↓
从剪贴板读取信息
    ↓
自动填入表单
    ↓
用户只需填写账号和密码
    ↓
点击保存
```

### 7. Markdown 格式化模块

**职责：** 在备注中支持格式化

**支持的格式：**
```
**粗体**      → 显示为加粗文字
*斜体*        → 显示为斜体文字
`代码`        → 显示为代码块
[链接](url)   → 显示为可点击的链接
```

**工作流程：**
```
用户在备注中输入 Markdown 语法
    ↓
保存记录
    ↓
显示时，调用 parseMarkdown() 函数
    ↓
用正则表达式识别 Markdown 语法
    ↓
转换成 HTML 代码
    ↓
在页面上显示格式化的文本
```

---

## 安全性分析

### ⚠️ 重要：这个应用的安全性限制

#### 1. 密码以明文存储
- **现状：** 密码没有加密，以明文形式存储在 localStorage
- **风险：** 如果有人能访问你的电脑，可以看到所有密码
- **建议：** 只在个人电脑上使用，不要在公共电脑上使用

#### 2. localStorage 的安全性
- **现状：** localStorage 中的数据可以被浏览器中运行的任何 JavaScript 代码访问
- **风险：** 恶意网站的 JavaScript 可能窃取数据（但这个应用是本地的，风险较低）
- **建议：** 不要在不信任的网站上使用这个应用

#### 3. 浏览器缓存
- **现状：** 如果浏览器缓存被清除，数据会丢失
- **风险：** 误操作导致数据丢失
- **建议：** 定期导出备份

### ✅ 安全的地方

#### 1. 数据不上传到服务器
- 所有数据都存在你的电脑上
- 没有人能从网络上窃取你的数据

#### 2. 没有网络请求
- 应用不会连接到任何外部服务
- 不会泄露你的信息

#### 3. 代码是开源的
- 你可以查看所有代码
- 没有隐藏的恶意代码

### 🛡️ 安全建议

1. **定期备份**
   - 每周导出一次备份
   - 保存在安全的地方（U盘、云盘等）

2. **使用强密码**
   - 为重要账号设置复杂密码
   - 定期更改密码

3. **保护你的电脑**
   - 安装杀毒软件
   - 定期更新操作系统和浏览器

4. **不在公共电脑上使用**
   - 网吧、图书馆等公共场所的电脑不要使用

5. **备份文件要加密**
   - 如果把备份文件放在云盘上，最好加密

---

## 常见问题

### Q1: 为什么数据没有加密？
**A:** 这个应用的目标是简单易用。加密会增加复杂性，而且数据本来就不离开你的电脑。如果你需要加密，可以：
- 把备份文件放在加密的文件夹里
- 使用 Windows BitLocker 或 Mac FileVault 加密整个硬盘

### Q2: 我可以在多个浏览器中使用吗？
**A:** 可以，但每个浏览器都有独立的 localStorage。所以：
- Chrome 中的数据和 Firefox 中的数据是分开的
- 如果你想在 Firefox 中使用 Chrome 的数据，需要导出后再导入

### Q3: 我可以在多个设备上使用吗？
**A:** 可以，但需要手动同步：
1. 在设备 A 上导出备份
2. 把备份文件复制到设备 B
3. 在设备 B 上导入备份

### Q4: localStorage 的容量是多少？
**A:** 通常是 5-10MB，取决于浏览器。这足以存储几千条记录。

### Q5: 如果我清除了浏览器数据怎么办？
**A:** 如果你之前导出过备份，可以导入恢复。如果没有备份，数据无法恢复。这就是为什么建议定期备份。

### Q6: 这个应用支持哪些浏览器？
**A:** 支持所有现代浏览器：
- Chrome 4+
- Firefox 3.5+
- Edge
- Safari
- 其他基于 Chromium 的浏览器

不支持 Internet Explorer（太老了）。

### Q7: 我可以修改代码吗？
**A:** 可以！代码是开源的，你可以：
- 修改样式（颜色、字体等）
- 添加新功能
- 修复 bug

### Q8: 应用会自动保存吗？
**A:** 是的。每次你添加、编辑或删除记录时，应用都会自动保存到 localStorage。

---

## 总结

### 这个应用的核心思想
```
简单 + 安全 + 本地 = 完美的个人密码管理器
```

### 技术特点
- 单文件架构 - 一个 HTML 文件包含所有代码
- 零依赖 - 不需要安装任何库或框架
- 纯前端 - 所有逻辑都在浏览器中运行
- 本地存储 - 数据不离开你的电脑

### 适用场景
- ✅ 个人使用
- ✅ 小团队共享（通过导出/导入）
- ✅ 离线使用
- ✅ 快速部署

### 不适用场景
- ❌ 企业级应用（需要更复杂的功能）
- ❌ 需要实时同步的场景
- ❌ 需要加密存储的场景
- ❌ 需要多用户协作的场景

---

**文档版本：** 1.0  
**最后更新：** 2026-05-28  
**作者：** Claude Code
